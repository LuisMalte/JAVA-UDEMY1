package org.lsmalte.pooclasesabstractas.form.validador.mensaeje;

public interface MensajeFormateable {
    public  String MensajeFormateado(String campo);

}
/*una interface es similar a una clase abstracta ya que al implementarla se necesita hacer
* el mismo metodo que tiene pero con la diferencia de que no hay una relacion de parentezco
* entre las clases que los implementas es decir cuaquiera puede tipo de objeto puede implmentar
* una interface pero no tendra ninguna relacion de parentezcomo como si pasa con las
* clases abstractas*/
